# binary-file-compressor-dsa
A C++ binary file compression utility executing Run-Length Encoding (RLE) built entirely from scratch using custom Min-Heap, BST, and Linked Queue data structures without STL reliance.
